import express from 'express';
import { clients } from '../ws/gameWebSocket'; // Import clients map

const router = express.Router();

// POST route to send a message to a specific WebSocket client
router.post('/init', );

export default router;